// Original file: validation.proto


/**
 * Reviewed Guardrail-local composition assertion; never inferred from observed output.
 */
export interface ValidationExpectedMatch {
  'policyId'?: (string);
  'ruleId'?: (string);
}

/**
 * Reviewed Guardrail-local composition assertion; never inferred from observed output.
 */
export interface ValidationExpectedMatch__Output {
  'policyId': (string);
  'ruleId': (string);
}
