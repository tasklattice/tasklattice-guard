import { randomUUID } from 'node:crypto';
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
import assert from 'node:assert/strict';
import { Pool } from 'pg';
import { drizzle } from 'drizzle-orm/node-postgres';
import { Registry } from 'prom-client';
import * as schema from '../server/db/schema.js';
import { loadConfig } from '../server/config.js';
import { ControlPlaneService } from '../server/services/control-plane.js';
import { ModelConfigurationService } from '../server/model-config/service.js';
import { createHttpApp } from '../server/http/app.js';
const url = process.env.GUARD_REVIEW_POSTGRES_URL!;
assert.equal(new URL(url).port,'55451');assert.equal(new URL(url).hostname,'127.0.0.1');
const ns = `api_review_${randomUUID().replaceAll('-','')}`;
const admin = new Pool({connectionString:url,max:1});let pool:Pool|undefined;const results:Record<string,unknown>={};
try {
 await admin.query(`CREATE SCHEMA "${ns}"`);pool=new Pool({connectionString:url,max:4,options:`-c search_path=${ns}`});
 const migration=(name:string)=>readFileSync(new URL(`../server/db/migrations/${name}`,import.meta.url),'utf8');
 for(const {tag} of JSON.parse(migration('meta/_journal.json')).entries) await pool.query(migration(`${tag}.sql`).replaceAll('"public".',`"${ns}".`));
 await pool.query(`INSERT INTO auth_user (id,name,email,role) VALUES ('admin','Fixture','api-review@example.test','admin');
 INSERT INTO controller_state (id) VALUES ('singleton');INSERT INTO runner_pool (id,name) VALUES ('default','Default');
 INSERT INTO guardrail (id,name,draft_config) VALUES ('source','Source','{}'),('copy','Copy','{}');
 INSERT INTO guardrail_version (guardrail_id,version,generation,status,runtime_profile,plan,artifact_id) VALUES ('source','1',1,'ready','auto','{}','artifact');
 INSERT INTO endpoint (id,name,adapter) VALUES ('endpoint','Fixture','HTTP');
 INSERT INTO guardrail_test_case (guardrail_id,id,name,policy_id,phase,content,expected_decision,origin) VALUES ('source','shared-case','Case','custom','input','fixture','allow','custom'),('copy','shared-case','Case','custom','input','fixture','allow','custom');`);
 const config=loadConfig({NODE_ENV:'test',CONTROLLER_DATABASE_URL:url,CONTROLLER_RUNNER_TOKEN:'runner-token-that-is-at-least-32-characters',CONTROLLER_ARTIFACT_SIGNING_KEY_PATH:'/tmp/unused-review-signing.pem',CONTROLLER_POLICY_CATALOG_DIR:resolve('../runner/toolkit/policy_library/assets'),BETTER_AUTH_SECRET:'synthetic-api-review-secret-that-is-long-enough'});
 const db=drizzle(pool,{schema});const service=new ControlPlaneService(db,config);
 const countCases=async()=>Number((await pool!.query('SELECT count(*) FROM guardrail_test_case')).rows[0].count);
 const cases=[await countCases()];await service.deleteTestCase({caseId:'shared-case',actorId:'admin'});cases.push(await countCases());await service.deleteTestCase({caseId:'shared-case',actorId:'admin'});cases.push(await countCases());
 assert.deepEqual(cases,[2,1,0]);results.duplicateCaseDeletion={rowsBeforeAndAfterIdenticalDeletes:cases};
 const draft={routes:[{id:'fallback',name:'Fallback',kind:'fallback' as const,enabled:true,selector:{expression:{combinator:'and' as const,conditions:[]}},targets:[{id:'target',guardrailId:'source',guardrailVersion:'1',weightBps:10000}]}]};
 const router=await service.trafficRouting.create('Fixture','',draft,'admin');
 const generation=async()=>Number((await pool!.query("SELECT desired_generation FROM controller_state WHERE id='singleton'")).rows[0].desired_generation);
 const generations=[await generation()];await service.trafficRouting.bind(router.id,['endpoint'],'admin');generations.push(await generation());await service.trafficRouting.bind(router.id,['endpoint'],'admin');generations.push(await generation());
 assert.equal(generations[2],generations[1]!+1);results.repeatedEndpointPut={generations};
 await service.trafficRouting.publish(router.id,1,'first-key','admin');
 const replay=await service.trafficRouting.publish(router.id,1,'first-key','admin',undefined,{routes:[]},[]);assert.equal(replay.activeRevision,1);results.changedReviewWithSameKey={accepted:true,activeRevision:replay.activeRevision};
 await service.trafficRouting.publish(router.id,1,'second-key','admin');const old=await service.trafficRouting.publish(router.id,1,'first-key','admin');assert.equal(old.activeRevision,2);results.oldPublishKeyReplay={originalRevision:1,returnedRevision:old.activeRevision};
 const models=new ModelConfigurationService(db,'synthetic-api-review-secret-that-is-long-enough',config.policyCatalogDir);
 const app=createHttpApp({config,auth:{api:{getSession:async()=>({user:{id:'admin',role:'user'}})}} as any,service,models,runnerControl:{distributionStatus:async()=>({distributionStatus:'ready',desiredGeneration:7})} as any,metrics:{registry:new Registry()} as any});
 const modelCount=async()=>Number((await pool!.query('SELECT count(*) FROM model_configuration_revision')).rows[0].count);
 const before=await modelCount();assert.equal((await app.request('/api/v1/model-configuration')).status,200);const after=await modelCount();assert.equal(before,0);assert.equal(after,1);results.getCreatesDraft={before,after};
 const activeId=randomUUID();await pool.query("INSERT INTO model_configuration_revision (id,revision,state,assignments,generation) VALUES ($1,2,'activating','{}',7)",[activeId]);
 assert.equal((await app.request('/api/v1/model-configuration')).status,200);const state=(await pool.query('SELECT state FROM model_configuration_revision WHERE id=$1',[activeId])).rows[0].state;assert.equal(state,'active');results.getByRegularSessionFinalizesActivation={before:'activating',after:state};
 writeFileSync(resolve('../docs/evidence/api-semantics-20260912/reproduction.json'),JSON.stringify({environment:'isolated temporary PostgreSQL schema on review fixture database; schema removed after run',...results},null,2)+'\n');console.log(JSON.stringify(results,null,2));
} finally {await pool?.end();await admin.query(`DROP SCHEMA IF EXISTS "${ns}" CASCADE`);await admin.end();}
