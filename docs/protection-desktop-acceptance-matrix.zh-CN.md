# 桌面验收合并矩阵

记录：2026-09-08 19:48。范围是既定保护产品化流程，不含移动端、任意浏览器/辅助技术组合、Retrieval、Dialog或真实工具授权。这里合并证据，不把历史部署或组件Mock说成本次真实浏览器通过。

## 本次新证据

- `npx vitest run --environment jsdom src`：62个文件，**280 passed / 1 failed**。失败位于`protection-library.test.tsx`的旧集合默认隐藏断言。用户已说明在另一个任务重新设计Policy侧栏筛选；保留新实现，不在本任务恢复旧开关，也不删除失败断言以制造全绿。筛选任务定稿后需同步该测试。
- 当前Controller `:dev`：`sha256:cef90f0fabcb0c8e3fc8ecf12f1914437efa402800446621458f93aa57ed7112`。本次没有构建、部署或模型调用。
- 当前1366×900创建器：11个步骤，8个保护目录均可选；五种预配置可选。银行预览显示“包含17项”，所有步骤仍未选择；点击应用后才显示内容10、隐私4、攻击2、行业1，其他目录仍未选择。
- 银行交付步骤显示固定完整缓冲、不可选原因和顺序语义；最终检查显示17 Policies、38 Rules、完整缓冲，以及“创建草稿不影响线上流量”。没有点击创建、验证或发布。截图和DOM确认横向不溢出。
- 当前Health：Default版本`20260908-024342.826Z`，Input32/Output32，32个本地Policy，2个可服务Runner，generation30。模型仅显示配置状态，不冒充推理健康或验证通过。实际刷新成功且浏览器error日志为空。刷新期间观察到焦点落到body，仍需单独验证键盘焦点连续性，不能据此签署键盘项。

## 证据分层

“组件”指当前上述测试运行及检查过的断言；“部署”指记录中明确的真实页面路径。历史部署证据只支持该版本及列明路径。`待合并`不是已复现功能缺陷；`设计协调`不授权恢复其他任务的旧设计。

| 对象/状态 | 当前组件证据 | 部署交互证据 | 判定及精确剩余项 |
| --- | --- | --- | --- |
| 名称与完整可选地图 | `create-guardrail-wizard.test.tsx`：仅名称、空地图禁用、跨目录选择保留、预配置预览与应用分离 | 本次11步骤、银行预览→应用→交付→检查 | 主路径通过；没有新增创建写入 |
| Policy目录加载/空/错误/恢复 | 向导测试：catalog reset的success/error/missing三分支；加载期间不误报缺失、不发preview/create；恢复保留绑定 | 既有延迟目录及503恢复代理记录 | 状态逻辑通过；独立于正在改版的Library侧栏 |
| 权限丢失/离线/未知保存结果 | 向导测试：权限变化禁止写入；离线不排队自动写；显式恢复保留payload | 既有tali权限代理及已提交保存响应丢失的人工核对路径 | 保留已验证范围；普通创建不是幂等自动重试保证 |
| 模型依赖/可选保护 | `protection-dependencies.test.tsx`：local不读模型设置、方向限定、unknown不冒充未配置、读取失败重试 | 本次银行纯本地预配置；Health本地Default与生效模型分开 | 已验证本地路径；未配置模型的状态主要为组件证据 |
| Policy与Rule顺序/Override | `policy-binding-editor.test.tsx`：固定旧版本、必需参数、方向、局部Rule排序与禁用、继承动作；`guardrail-detail.test.tsx`检查保存payload | 历史银行草稿实际排序/保存/重载；本次顺序说明及绑定来源可见 | 主编辑链路有证据，不修改源Policy |
| 流式交付/最终检查 | 向导完整缓冲payload；独立数据面与Relay三模式合同；实际模型11/11报告 | 本次银行因完整内容要求锁定full buffered并解释原因 | 业务含义与运行证据一致；不把window与全文判断等同 |
| Library侧栏分类/筛选 | 新`policy-library.test.tsx`覆盖分组筛选；旧`protection-library.test.tsx`默认隐藏集合断言失败 | 用户另一个任务正在重设计 | **设计协调**：不覆盖新实现，待定稿同步旧测试；当前全前端门禁不是绿色 |
| 固定Policy版本详情 | `protection-library.test.tsx`前两项：打开指定旧版本；缺失时不替代且可关闭恢复 | 历史Policy详情检查 | 组件通过；当前筛选改版后的完整深链浏览器路径待合并 |
| Studio目录/验证/会话/焦点 | `policy-studio.test.tsx`与`policy-library-focus.test.tsx`：目录503/空/离线、验证旧revision、关闭重开、导入/编辑入口 | 历史真实目录失败恢复、旧验证结果及新建关闭焦点 | 导入和嵌套编辑焦点仅组件证据；不签署实际文件选择器路径 |
| Policy发布两种故障 | 上轮31项前端及15项真实数据库边界；本次Studio/API/Library相关测试继续通过 | 明确发布成功后GET503→只读恢复；提交成功但POST响应丢失→revision1重试，数据库仅一版本/一审计 | 两条具体故障路径已通过，不再列为未实现 |
| Health加载/unknown/恢复 | `status.test.tsx`：8项覆盖就绪、未配置模型、同步中、无响应、新草稿失败不影响已发布、无依赖证据、失败刷新消除绿态 | 本次真实已发布覆盖与刷新；15秒观察机制 | 数据语义通过；刷新键盘焦点连续性待验证 |
| Findings严重度空筛选 | `guardrail-detail.test.tsx`：重复局部id的2条Medium→Critical0后DOM为0→Medium恢复2 | 既有发现页面证据 | 当前组件通过；本次未造流量或写审计来重新构造样例 |

## 有限的后续清单

1. 等另一个任务完成Policy筛选设计，再同步旧集合断言及执行该页深链、空筛选、恢复路径；不能提前替对方选设计。
2. 验证Health刷新前/中/后键盘焦点是否连续；若确认丢失，仅修该按钮状态，不重做页面。
3. 将实际导入文件、嵌套编辑退出路径补入浏览器证据，区别于现有组件测试。

以上是当前桌面合并项，不把“所有高级组合”作为无限追加开发的理由。完整U1在这三项与整体回归复核前仍开放。C1动态依赖保证、Q1独立质量集/标签审核是不同合同项，此矩阵不替代它们。

Vibe Designing评估：product-console/release_gate，沿用20/20/15/25/15/5和8分无关键阻断门槛。本次为证据合并，不凭局部截图给全产品打分；筛选验收冲突与焦点证据不足使完整门禁不通过。决策`stop_needs_input`仅针对另一任务拥有的筛选设计；其他两项仍可独立推进。没有新Git提交、没有真实API调用。
